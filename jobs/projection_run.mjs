/* RETIRED. A stale near-duplicate of jobs/projections_run.mjs (note the plural). Running this by
   mistake would write projections from old code, so it now refuses. */
console.error("jobs/projection_run.mjs is retired. Use jobs/projections_run.mjs.");
process.exit(1);
