/* RETIRED, imported by nothing.
   HeadlineBoxes was superseded by the xPTS box above the pitch. PlayerPicker was superseded by the shared
   Candidates list. A zip cannot delete, so both are inert; the tidy workflow removes them properly. */
export default function Retired() { return null; }
