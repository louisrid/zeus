/* RETIRED. Superseded by the shared pitch and player list: the Squad screen now uses the same
   BuilderPitch and Candidates components as the Builder, so a squad and a swap work identically
   wherever you are. A zip cannot delete, so this is inert. */
export default function Retired() { return null; }
