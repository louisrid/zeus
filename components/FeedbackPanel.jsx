/* RETIRED, imported by nothing. Superseded by BuilderPitch and Checks. A zip cannot delete, so this is
   inert; the tidy workflow removes it properly. */
export default function Retired() { return null; }
