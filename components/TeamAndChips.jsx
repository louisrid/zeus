/* RETIRED, rendered nowhere. The team connect box was removed from the Squad screen: team 4812 loads
   itself. A zip cannot delete, so this is inert; the tidy workflow removes it. */
export default function Retired() { return null; }
